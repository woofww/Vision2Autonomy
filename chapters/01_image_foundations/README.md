# Chapter 01: Image foundations

This chapter starts with the numerical representation of grayscale images and
builds two-dimensional convolution and Gaussian smoothing using NumPy. The
reusable implementation lives in `src/vision2autonomy/image`; the tests compare
the vectorized convolution with a deliberately simple loop-based reference.

